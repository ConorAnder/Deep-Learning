If the model is too simple for the data we are trying to fit, this is known as [[Underfitting]].
This can happen if the order of the model is not large enough to fit the data.

This can be spotted by looking at the loss graphs and keeping an eye on whether the [training loss](Error) remains high.